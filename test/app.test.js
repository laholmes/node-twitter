const assert = require('assert');

describe('app', () => {
  describe('rest count ', () => {
    it('should return accurate hashtag count', () => {
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    });
  });
  
  describe('streaming count', () => {
      
  })
});